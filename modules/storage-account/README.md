# modules/storage-account

This module provisions an Azure Storage Account and follows the repository naming
convention. It expects `resource_group_name`, `project_name`, `env`, and optional
`running_number` inputs.
